# Arquivo de ambiente para simular o modo de produção (Firebase).
#
# Use este arquivo para desenvolver e testar localmente com as configurações
# de produção, como a API real hospedada na Render.
#
# Para usar este modo, execute o build com a flag --mode:
# > npm run build -- --mode firebase

# IMPORTANTE: Substitua pela URL da sua API em produção na Render.
VITE_API_URL=https://sua-api.onrender.com

# Você pode adicionar outras chaves de API de produção aqui
# Ex: VITE_STRIPE_PUBLIC_KEY=pk_live_suachaveaqui
